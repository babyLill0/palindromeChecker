import { FC } from 'react'

const Header: FC = () => {
  return (
    <header className="flex h-24 items-center justify-center bg-customPink p-3 text-2xl font-bold"> 
      Palindrome checker
    </header>
  )
}

export default Header;